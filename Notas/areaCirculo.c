#include <stdio.h>
#include <stdlib.h>
/* run this program using the console pauser or add your own getch, system("pause") or input loop */
float _radius1, $area$;
// variables pueden iniciar con letra o _ o $,
// despues del primer caracter si pueden colocar numeros:
// int 5a;    INCORRECTA
// int a5;    CORRECTA
int main(int argc, char *argv[]) {
	int a5;
	_radius1 = 1;
	$area$ = 3.14159 * _radius1 * _radius1;
	printf("\n\n \"%crea\" = %E",181,  $area$);
	return 0;
}

